9
first feature is 1 iff boolean is satisfied
    so on...

10
subscript 1 corresponds to one matrix, 2 corresponds to other matrix
    - one matrix being emission matrix
    - other matrix being transition matrix

11
change in notation -> looks like standard linear model on some feature representation

13

